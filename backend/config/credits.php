<?php

return [
    // Crédit minimum requis pour un trajet
    'min_cost' => 3,
    // Commission fixe prélevée par la plateforme (en crédits)
    'commission_fee' => 2,
    // Diviseur pour calculer le coût maximal d'un trajet (distance / divisor)
    'max_distance_divisor' => 3,
    // Seuil de solde en dessous duquel une alerte est déclenchée
    'alert_threshold' => 0,
]; 